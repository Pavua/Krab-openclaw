#!/bin/zsh
# -----------------------------------------------------------------------------
# One-click враппер для диагностики транскрибатора.
# -----------------------------------------------------------------------------

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$ROOT_DIR/scripts/transcriber_doctor.command" "$@"

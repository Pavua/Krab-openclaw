# Weekly Recap — 2026-W18 (2026-04-26 → 2026-05-03)

## Stats
- **Commits:** 263
- **Diff:** +155 210 / −61 135 (675 файлов)
- **Categories:** feat ~80 · fix ~75 · refactor ~28 · docs ~25 · test ~15 · merge ~6

## Top 5 impactful

1. **SkillCurator** (Waves 14-I → 16-D) — dry-run analyzer → LLM proposer (gemini-3-flash) → `apply_with_approval` + mutex + archive → A/B framework (round-robin + decision).
2. **Hermes Phase A/B** — standalone сервис + LaunchAgent plist; ACP bridge foundation; `agent_engine` router.
3. **OpenClaw concurrency backpressure** — semaphore limiter (Wave 14-B), фикс перегрузки gateway.
4. **Bug 14 hard response cap** — анти-«бесконечный typing», subtask-aware fallback, UX-формулировка без implied failure.
5. **SQLite resilience** — `PRAGMA synchronous=FULL` + `wal_autocheckpoint` (torn-page prevention), `WAL TRUNCATE` on graceful shutdown, корректная загрузка `sqlite-vec` extension в `open_archive()`.

## Notable also

- LM Studio Bearer token support + setup script
- `forward_batch` coalescing (single AI-call для batch+photos)
- Sentry per-session dedupe + 503 wrap + CancelledError frame-aware narrowing
- Hallucination guard для fabricated tool-success reports
- Userbot предпочтительнее bot-API для personal DMs (Дашуля incident)
- `tool_calls_executed` parsing (Wave 11-C contract на Krab-side)

## Refactor / hygiene

- 20× `refactor(commands)` — массовая нормализация регистра команд
- DEAD CODE cleanup (handle_filter/mem/setpanelauth/top — wired либо удалены)
- Resolver wire-up для send_photo/voice/reaction MCP-tools

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
addopts = "-v --tb=short -W \"ignore:There is no current event loop:DeprecationWarning\""
filterwarnings = [
    # Внешняя библиотека pyrogram использует устаревший API event loop.
    "ignore:There is no current event loop:DeprecationWarning:pyrogram\\.sync",
    "ignore:There is no current event loop:DeprecationWarning",
]

[tool.coverage.run]
source = ["src"]
omit = ["tests/*", "**/__pycache__/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]  # line too long

[project]
name = "kraab"
version = "1.0.0"
description = "🦀 Краб - AI Userbot с интеграцией OpenClaw и LM Studio"
requires-python = ">=3.11"

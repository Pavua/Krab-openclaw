# Krab Skills Registry RU

## Что считать практическими инструментами проекта

- `.command`-launchers в корне репозитория;
- `scripts/check_*`, `scripts/openclaw_*`, `scripts/runtime_*`, `scripts/live_*`;
- owner panel `:8080`;
- native dashboard `:18789`;
- `Start Full Ecosystem.command`, `Release Gate.command`, `Export Handoff Bundle.command`.

## Что использовать по умолчанию

- для multi-account readiness: `Check New Account Readiness.command`;
- для runtime ownership truth: `Check Current Account Runtime.command`;
- для handoff: `Export Handoff Bundle.command`;
- для translator mobile packet: `Build Translator Mobile Onboarding Packet.command`.

# 🦀 Krab Phase 10: Swarm & MCP Singularity

## 🎯 Цели (Goals)
1. **MCP Integration**: Позволить Крабу использовать внешние инструменты через Model Context Protocol (MCP).
2. **Swarm Intelligence**: Параллельное выполнение задач на разных моделях (например, Gemini для зрения + Local LLM для текста).
3. **Autonomous Learning**: Бот может самостоятельно читать документацию (MCP Documentation server) и обучаться новым функциям.
4. **Resilience**: Улучшенная обработка ошибок и самовосстановление через `SelfRefactor`.

## 🛠 Технический стек
- **Protocol**: MCP (Model Context Protocol) via `mcp` SDK.
- **Async**: `asyncio` для параллельных вызовов "Роя" (Swarm).
- **Security**: Изолированные сессии для выполнения инструментов.

## 📋 План реализации
### Шаг 1: MCP Client (Core)
- Реализовать `MCPManager` в `src/core/mcp_client.py`.
- Поддержка подключения к локальным серверам (через `stdio`).
- Интеграция MCP-инструментов в `ToolHandler`.

### Шаг 2: Swarm Engine
- Реализовать `SwarmOrchestrator`.
- Метод `parallel_exec`: запускает цепочку инструментов одновременно.
- Агрегатор ответов: собирает данные из разных источников в один контекст.

### Шаг 3: Верификация
- Тесты подключения к тестовому MCP-серверу.
- Сценарий: Краб использует MCP-калькулятор или MCP-поиск.

---
*Документ создан в рамках Фазы 10 (Февраль 2026).*

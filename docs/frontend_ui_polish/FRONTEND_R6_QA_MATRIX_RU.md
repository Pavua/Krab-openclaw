# Матрица тестирования Frontend R6: Mobile, a11y & States

Этот документ содержит матрицу проверок (QA Matrix) для верификации реализации **Frontend R6**. Он предназначен для ручного тестирования и проверки прототипов Krab Web Panel V2 (`index_redesign.html`, `transcriber_console.html`, `ops_center.html`).

## 📱 Блок 1: Мобильная адаптивность (Viewport < 420px)

| ID | Компонент / Страница | Проверяемый сценарий | Ожидаемый результат | Статус |
| --- | --- | --- | --- | --- |
| M-01 | **Все 3 страницы** | Горизонтальный скроллинг | При уменьшении ширины экрана до 320px-390px (iPhone SE / 12) на страницах **нет** нежелательного горизонтального скролла (`overflow-x`). Все элементы умещаются в ширину экрана. | [ ] |
| M-02 | **Index (`index_redesign.html`)** | Блок "Система" длинные имена (Local Model) | Если выбрана длинная локальная модель (например, `llama-3-8b-instruct-v2-q4_k_m.gguf`), текст не вылезает за пределы карточки, а переносится или обрезается многоточием (`text-overflow: ellipsis`, `break-word`). | [ ] |
| M-03 | **Index (`index_redesign.html`)** | Grid дашборда на смартфонах | Карточки выстраиваются в одну колонку. Отступы не слишком гигантские (padding карточек уменьшен до 16px). | [ ] |
| M-04 | **Transcriber (`transcriber_console.html`)** | Тулбар контроллера (Audio Controls) | Кнопки управления (Record, Stop, Config) и выбор устройства не "сплющиваются" до неузнаваемости, а адекватно переносятся на новые строки или занимают всю ширину. | [ ] |
| M-05 | **Ops Center (`ops_center.html`)** | Строки Журнала (Ops History) и Алерта | Длинные сообщения об ошибках (например, JSON трейсбек) переносятся (`word-break: break-all` / `break-word`) и не растягивают таблицу/блок за рамки экрана. | [ ] |

## ♿ Блок 2: Доступность (Accessibility - a11y)

| ID | Компонент / Страница | Проверяемый сценарий | Ожидаемый результат | Статус |
| --- | --- | --- | --- | --- |
| A-01 | **Кнопки без текста (иконки)** | Наличие `aria-label` | Все кнопки, содержащие только SVG/иконки (например, переключение темы, Back to Console, Record, Stop), имеют осмысленный атрибут `aria-label` для скринридеров. | [ ] |
| A-02 | **Навигация с клавиатуры** | Focus Rings (`:focus-visible`) | При навигации клавишей \<Tab\> активные элементы (кнопки, инпуты, селекты) получают четко видимую синюю/контрастную обводку (Focus Ring, заданную через `nano_theme.css`). | [ ] |
| A-03 | **Динамические регионы** | `aria-live` атрибуты | Блоки, обновляющиеся динамически без перезагрузки (System Status в Ops Center, Assistant Output), имеют атрибут `aria-live="polite"` или `assertive` для оповещения скринридеров об изменении контента. | [ ] |
| A-04 | **Семантика** | Структура заголовков и ролей | Страницы используют логичную иерархию заголовков (`h1` - Web Panel, `h2/h3` - блоки). Алертам назначен `role="alert"`. | [ ] |
| A-05 | **Цветовой контраст** | Читаемость текстов и бейджей | Тексты на цветных бейджах (`.badge.ok`, `.badge.warn`, `.badge.bad`) остаются легко читаемыми как в темной, так и в светлой темах (контраст не менее 4.5:1 для нормального текста). | [ ] |

## 🔄 Блок 3: Унификация состояний (States)

| ID | Компонент / Страница | Проверяемый сценарий | Ожидаемый результат | Статус |
| --- | --- | --- | --- | --- |
| S-01 | **Все 3 страницы** | Ожидание данных (Loading State) | Во время загрузки данных или ожидания ответа отображается стандартизированное пустое состояние "Awaiting Input" или пульсирующий индикатор загрузки, а не "рваный" интерфейс. | [ ] |
| S-02 | **Index (`index_redesign.html`)** | Пустая история Ops (`#opsHistory`) | При отсутствии истории Ops отображается `.empty-state` с иконкой документа и текстом "История ops: пусто", а не просто пустая строка. Оформление идентично пустой транскрипции в Transcriber. | [ ] |
| S-03 | **Index / Transcriber** | Пустой вывод ассистента (`#assistantOutput`) | При загрузке до первого запроса в блоке вывода ассистента отображается состояние `.empty-state` (с центрированной иконкой микрофона и текстом "Awaiting Input"). | [ ] |
| S-04 | **Transcriber (`transcriber_console.html`)**| Состояние ошибки сервера / отключения | При обрыве связи (mock network error) UI отображает заметное сообщение об ошибке (возможно через `.badge.bad` или красный `empty-state`), предлагая действие для повтора (Retry). | [ ] |

---
**Инструкция по проведению тестирования:**

1. Открыть тестируемый HTML-файл прототипа в Chrome/Safari.
2. Проверить адаптивность через инструменты разработчика (Device Mode: iPhone 12/SE).
3. Проверить навигацию клавиатурой (только клавиша Tab, Shift+Tab и Enter).
4. Заполнить колонку "Статус" (✅ Pass, ❌ Fail, ⚠️ Warn).

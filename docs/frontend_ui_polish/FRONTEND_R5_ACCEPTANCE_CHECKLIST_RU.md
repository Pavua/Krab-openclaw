# Чеклист интеграции Frontend R5

Данный документ содержит проверки готовности UI-компонентов к переносу в основной боевой `src/web/index.html`. Должно быть выполнено 100% чеклиста перед промоушеном.

## 1. Универсальные стили (`nano_theme.css`)

- [ ] **PASS / FAIL**: Наличие классов `.state-success`, `.state-error`, `.state-warn` и их модификаторов текста без переопределения в HTML-коде.
- [ ] **PASS / FAIL**: Наличие CSS-анимаций `.pulse-ok`, `.pulse-warn`, `.pulse-error`, `.pulse-ai`.
- [ ] **PASS / FAIL**: Корректная работа `.text-ellipsis` и `.word-wrap` для предотвращения разрыва верстки длинными текстами.

## 2. Transcriber Console (`transcriber_console.html`)

- [ ] **PASS / FAIL**: Визуальная иерархия сохраняется, цвета соответствуют токенам Deep Space.
- [ ] **PASS / FAIL**: Состояние **Idle**: серая индикация, кнопка "Start Recording".
- [ ] **PASS / FAIL**: Состояние **Listening**: зеленая пульсирующая индикация `.pulse-ok`, кнопка "Stop Recording" (красная).
- [ ] **PASS / FAIL**: Состояние **Processing (AI)**: фиолетовая индикация `.pulse-ai` имитирующая обработку текста.
- [ ] **PASS / FAIL**: Состояние **Error**: красная индикация `.pulse-error`, текст ошибки.
- [ ] **PASS / FAIL**: Мобильная версия (<420px): `header` и `layout` перестраиваются в колонку 1fr, без горизонтального скролла с сохранением читаемости окна чата.
- [ ] **PASS / FAIL**: Состояние **Empty**: при отсутствии распознанного текста показывается placeholder with icon "Waiting for audio stream".

## 3. Ops Center (`ops_center.html`)

- [ ] **PASS / FAIL**: Состояние **Emergency**: красный пульсирующий бейдж глобального состояния системы и рамка.
- [ ] **PASS / FAIL**: Иерархия алертов: Активные алерты четко выделены красным/желтым (border-left), после `Ack` уходят в серый (opacity: 0.5) фон.
- [ ] **PASS / FAIL**: Состояние **Empty Journal**: отображение заглушки "No Journal Events", когда лог пуст.
- [ ] **PASS / FAIL**: Длинные тексты логов не разрывают таблицу; `text-ellipsis` и `word-wrap` отрабатывают там, где необходимо.

## Что проверить перед промоушеном в `index.html`

- [ ] Скрипт совместимости не выдает ошибок.
- [ ] Классы состояний (типа `state-error`) не конфликтуют с уже существующими классами в других модулях (например `toast` или `modal`).
- [ ] Отсутствие инлайн-стилей состояний в JS, все переключения должны делаться через добавление/удаление классов `.state-*` из темы.

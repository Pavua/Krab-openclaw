# 🦀 Краб - AI Userbot

Персональный AI-ассистент на базе OpenClaw с доступом через Telegram Userbot.

### ❓ Troubleshooting (Проблемы)

**Код не приходит?**
1. **Service Notifications**: Проверь чат "Telegram" (с синей галочкой) в приложении на телефоне/компе. Код часто приходит туда, а не в SMS.
2. **Flood Wait**: Если пробовал входить слишком часто, Telegram мог заблокировать отправку кодов на 1-24 часа. Просто подожди.
3. **VPN**: Попробуй сменить IP (включить/выключить VPN).
4. **Формат номера**: Вводи номер с `+` (например `+7999...` или `+1...`).

**Ошибка "Client is already terminated"?**
Это нормально при принудительной остановке (Ctrl+C).

### 🛠 Установка вручную

```bash
# Установить зависимости Python
pip install -r requirements.txt

# Настроить переменные окружения
cp .env.example .env
# Отредактируйте .env файл с вашими credentials

# Запустить
python src/main.py
```

## Структура

```
Краб/
├── src/
│   ├── main.py              # Точка входа
│   ├── userbot_bridge.py    # Telegram Userbot (Pyrogram)
│   ├── model_manager.py     # Управление моделями LM Studio
│   ├── openclaw_client.py   # Клиент OpenClaw Gateway
│   └── config.py            # Конфигурация
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── config/
│   └── models.yaml          # Конфигурация моделей
└── scripts/
    └── start.sh             # Скрипт запуска
```

## Конфигурация

Переменные окружения в `.env`:

```env
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash
TELEGRAM_SESSION_NAME=kraab
OPENCLAW_URL=http://127.0.0.1:18789
OPENCLAW_TOKEN=your_token
LM_STUDIO_URL=http://192.168.0.171:1234
```

## Команды

| Команда | Описание |
|---------|----------|
| `!краб привет` | Начать диалог |
| `!model list` | Список моделей |
| `!model switch <name>` | Переключить модель |
| `!status` | Статус системы |

## Тестирование

```bash
# Все тесты
pytest tests/ -v

# С покрытием
pytest tests/ --cov=src --cov-report=html
```

## Лицензия

MIT

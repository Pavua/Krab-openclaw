
# 🦀 Краб (Krab) v2.0 - Elite AI Userbot

**Персональный AI-ассистент, интегрированный в ваш Telegram-аккаунт.**
Работает на базе OpenClaw Gateway, LM Studio и Google Gemini. Полностью локален, автономен и расширяем.

---

## 🚀 Быстрый старт (macOS)

Просто дважды кликните по файлу:
👉 **`start_krab.command`**

Скрипт сам:
1. Запустит OpenClaw Gateway (порт 18792).
2. Активирует виртуальное окружение.
3. Запустит бота с системой авто-перезагрузки (Self-Healing).

---

## 🔥 Ключевые возможности

### 🧠 Искусственный Интеллект (OpenClaw)
- **Умные ответы**: Отвечает на сообщения, понимает контекст переписки.
- **Vision**: Видит и описывает фотографии, которые вы ему присылаете.
- **Мульти-модели**: Поддержка Gemini 2.0 Flash, GPT-4o и локальных моделей через LM Studio.
- **Стриминг**: Ответ печатается в реальном времени ("печатает...").

### 🎭 Система Ролей (Personas)
Превращайте бота в эксперта одной командой:
- `!role coder` — Python/JS сеньор.
- `!role security` — Специалист по кибербезопасности.
- `!role crypto_analyst` — Аналитик рынка.
- **Свои агенты**: `!agent new <имя> <промпт>` — создайте своего уникального помощника, и он сохранится навсегда.

### 🌐 Web-интеграция
- **Web Control**: `!web screen` — сделает скриншот открытой страницы в браузере.
- **Login**: `!web login` — откроет браузер для ручного входа в ChatGPT/Gemini.
- **Поиск**: `!search <запрос>` — найдет актуальную информацию в Brave Search (без рекламы).

### 🛠 Управление системой
- **Память**: `!remember <файл>` / `!recall <запрос>` — долгосрочная векторная память.
- **Файлы**: `!ls`, `!read`, `!write` — управление файлами проекта прямо из чата.
- **Диагностика**: `!diagnose` — полная проверка здоровья всех систем (OpenClaw, LM Studio, Config).
- **Саморазвитие**: Роль `sys_developer` позволяет боту читать и улучшать свой собственный код.

---

## 📚 Полный список команд

| Команда | Описание |
|---------|----------|
| **AI** | |
| `!краб <текст>` | Задать вопрос AI (или просто напишите "Краб, ...") |
| `!role <role>` | Сменить личность (coder, writer, sMM, etc.) |
| `!agent new` | Создать нового кастомного агента |
| `!voice` | Включить/выключить голосовые ответы |
| **Tools** | |
| `!search <q>` | Поиск в интернете (Brave) |
| `!web screen` | Скриншот браузера |
| `!remember` | Запомнить факт в базу знаний |
| `!recall` | Вспомнить информацию |
| **System** | |
| `!status` | Статус ресурсов (RAM, Model) |
| `!diagnose` | Проверка соединений и API |
| `!ls / !read` | Работа с файловой системой |
| `!restart` | Перезагрузка бота |

---

## 📂 Структура проекта

```
Krab/
├── start_krab.command   # Лаунчер (One-Click)
├── src/
│   ├── main.py          # Точка входа
│   ├── userbot_bridge.py# Логика Telegram (Pyrogram)
│   ├── openclaw_client.py # Клиент AI Gateway
│   ├── employee_templates.py # Роли и Агенты
│   ├── web_session.py   # Управление браузером (Playwright)
│   ├── mcp_client.py    # Интеграция с инструментами (MCP)
│   └── config.py        # Настройки
├── skills/              # Модули навыков (Self-Update и др.)
├── roles.json           # Сохраненные агенты пользователя
└── tests/               # Авто-тесты (Pytest)
```

## 🛡 Безопасность

- Бот отвечает **только** владельцу (Вам) и пользователям из `ALLOWED_USERS`.
- Файловые операции ограничены рабочей директорией (по умолчанию).
- Токены хранятся в `.env` и не попадают в логи.

---

**Developed by Antigravity (Ralph Mode).**
